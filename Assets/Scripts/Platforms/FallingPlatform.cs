using UnityEngine;
using System.Collections;

public class FallingPlatform : MonoBehaviour
{
	//Temporary platforms that have a duration that the player can stand that them before they fall  
	//These are shortcut platforms which, if the player does not jump off, they won't be able use them when they respawn

	// Use this for initialization
	void Start ()
	{
	
	}

	public void MovePlatform(Switch _switch) { }
	//public bool HasNeutralGravity { get { return hasNeutralGravity; } }
}

